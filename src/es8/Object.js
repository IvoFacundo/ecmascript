const data = {
  frontend: 'Emanuel',
  backend: 'Ivo',
  designer: 'CJ'
}

const entries = Object.entries(data)
console.log(entries)

const values = Object.values(data)
console.log(values)

const keys = Object.keys(data)
console.log(keys)
